spring:
    datasource:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/upflow_feedback_dev}
        username: ${DB_USERNAME:postgres}
        password: ${DB_PASSWORD:postgres}

    jpa:
        show-sql: on
        properties:
            hibernate:
                format_sql: on